# =============================================================================
# 天气与出行助手智能体系统 — Python 依赖清单
# =============================================================================
# 安装方式：
#   pip install -r requirements.txt
#
# 推荐使用虚拟环境：
#   python -m venv venv
#   venv\Scripts\activate       # Windows
#   source venv/bin/activate    # Linux/Mac
#   pip install -r requirements.txt
# =============================================================================

# -------------------------------- Web 框架 --------------------------------
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.30.0,<1.0.0

# -------------------------------- 数据库 --------------------------------
sqlalchemy>=2.0.30,<3.0.0

# -------------------------------- 配置管理 --------------------------------
pydantic>=2.7.0,<3.0.0
pydantic-settings>=2.3.0,<3.0.0
python-dotenv>=1.0.0,<2.0.0

# -------------------------------- 日志 --------------------------------
loguru>=0.7.0,<1.0.0

# -------------------------------- LangChain 核心 --------------------------------
langchain>=0.3.0,<1.0.0
langchain-core>=0.3.0,<1.0.0
langchain-openai>=0.2.0,<1.0.0        # OpenAI 兼容接口（智谱/DeepSeek 等）
langchain-community>=0.3.0,<1.0.0     # 社区模型（ChatZhipuAI 等）
langgraph>=0.2.0,<1.0.0              # LangGraph StateGraph（Agent 核心）
pyjwt>=2.8.0                          # ChatZhipuAI 依赖的 JWT 签名

# -------------------------------- HTTP 客户端 --------------------------------
requests>=2.32.0,<3.0.0

# -------------------------------- 类型提示（开发依赖，可选） --------------------------------
# typing-extensions>=4.12.0,<5.0.0
